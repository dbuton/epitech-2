{% extends 'base.html.twig' %}


{% block content %}

    <div class="row">
        {{ form_start(form, {'action': path('app_student_add'), 'method': 'POST'}) }}
        {{ form(form) }}
        {{ form_end(form) }}
    </div>
    <hr>

{% endblock %}
